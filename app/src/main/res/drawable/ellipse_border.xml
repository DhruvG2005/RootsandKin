<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="#FFDDC1"/> <!-- Background color -->
    <corners android:radius="25dp"/>  <!-- Ellipse shape -->
    <padding
        android:left="10dp"
        android:top="5dp"
        android:right="10dp"
        android:bottom="5dp" />  <!-- Padding inside the ellipse -->
</shape>
